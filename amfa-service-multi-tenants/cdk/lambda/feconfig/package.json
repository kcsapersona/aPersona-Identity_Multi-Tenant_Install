{
  "name": "feconfig",
  "version": "1.0.0",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf dist && esbuild ./*.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --outdir=dist --external:@aws-sdk/* --external:tenant-utils",
    "test": "echo \"no test\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
