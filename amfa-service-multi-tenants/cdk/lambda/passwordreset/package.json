{
	"name": "amfa",
	"version": "1.0.0",
	"description": "",
	"main": "passwordreset.mjs",
	"scripts": {
    "build": "rm -rf dist && esbuild ./*.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --format=esm --out-extension:.js=.mjs --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\" --outdir=dist --external:@aws-sdk/* --external:tenant-utils",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"esbuild": "^0.23.0"
	},
	"dependencies": {
		"nodemailer": "^6.9.4"
	}
}
