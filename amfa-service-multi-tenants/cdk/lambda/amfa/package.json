{
  "name": "amfa",
  "version": "1.0.0",
  "description": "",
  "main": "amfa.mjs",
  "scripts": {
    "build": "rm -rf dist && esbuild ./*.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --format=esm --out-extension:.js=.mjs --outdir=dist --external:@aws-sdk/* --external:tenant-utils",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "nodemailer": "^7.0.11",
    "otplib": "^12.0.1"
  }
}
