{
  "name": "ad-sync-dispatcher",
  "version": "1.0.0",
  "description": "AD Sync Dispatcher Lambda — fan-out to workers",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf dist && esbuild index.mjs routing.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --outdir=dist --external:@aws-sdk/*",
    "test": "node --test ./*.test.mjs"
  },
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.0.0",
    "esbuild": "^0.25.0"
  }
}
