{
  "name": "ad-sync-single-user",
  "version": "1.0.0",
  "description": "AD Sync Single-User Lambda — event-driven sync",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf dist && esbuild ./*.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --outdir=dist --external:@aws-sdk/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "ldapjs": "^3.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
