{
  "name": "organizationslist",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf dist && esbuild ./index.mjs ./crud.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --outdir=dist --external:@aws-sdk/* --external:admin-auth",
    "test": "node --test ./*.test.mjs"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
