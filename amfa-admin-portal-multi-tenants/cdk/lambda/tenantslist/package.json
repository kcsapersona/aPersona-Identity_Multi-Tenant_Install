{
  "name": "tenantslist",
  "version": "1.0.0",
  "description": "Lambda function for tenant CRUD operations with organization support",
  "type": "module",
  "main": "index.mjs",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-lambda": "^3.0.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && esbuild ./*.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --outdir=dist --external:@aws-sdk/* --external:admin-auth"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
