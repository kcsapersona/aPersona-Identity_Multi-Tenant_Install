{
  "name": "vpnconfig",
  "version": "1.0.0",
  "description": "VPN Config Lambda — manage per-tenant Site-to-Site VPN connections",
  "main": "index.mjs",
  "scripts": {
    "build": "rm -rf dist && esbuild ./*.mjs --entry-names=./[name] --bundle --minify --sourcemap --platform=node --target=node20.16 --outdir=dist --external:@aws-sdk/* --external:admin-auth",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
